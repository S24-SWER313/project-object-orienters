import React from 'react';

function PostText({text}) {
    return (
        <div>
            <p>
                {text}
            </p>
        </div>
    );
}

//props ->
//function #### convert to tag link

export default PostText;
